package custodybattle;

import edu.macalester.graphics.CanvasWindow;

//Class manages interactions of the paddle
public class PaddleManager {
    public Ball ball;
    // private boolean lawyer1Appears = false;
    // private boolean lawyer2Appears = false;
    private Paddle paddle;

    // public void intersectsPaddle(Ball ball) {
    //     if (paddle.testHit(ball.ballLeftSide())) {
    //         ball.positiveXVel();
    //     } else if (paddle.testHit(ball.ballRightSide())) {
    //         ball.negativeXVel();
    //     } else if (paddle.testHit(ball.ballTopSide())) {
    //         ball.positiveYVel();
    //     } else if (paddle.testHit(ball.ballBottomSide())) {
    //         ball.negativeYVel();
    //     }
    // }

    // public void makeLawyer1Paddle(CanvasWindow canvas) {
    //     canvas.add(lawyer1)
    // }

}
